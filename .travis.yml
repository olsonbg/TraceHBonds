dist: trusty
sudo: false
language: cpp
addons:
  apt:
    packages:
      - libboost-iostreams-dev
      - doxygen
      - graphviz
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make && make docs
